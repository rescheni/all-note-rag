# Changelog


## Unreleased

- **同步更安静**：API 自动 tick 从 30 秒改为约 1 小时，且仅当 `last_sync_at` 过期才入队；手动同步与 S3 变更唤醒不变。
- **个人笔记**：主导航与首页去掉「成员」/团队空间创建；ACL 文案改为仅自己；子账号改到账号页。
## 2026-09 — 枢纽体验与 Agent / 多源打磨

- **书架 UX**：连接与笔记的立体书架、文件夹树、可返回的面包屑；路径尽量显示标题（人话化）。
- **双链与块引用**：Wiki / 思源块引用 / Notion·飞书页面提及解析为站内链接，预览带反向链接。
- **主题**：抹茶纸色、墨夜、素白；设置页切换并持久化。
- **PWA**：manifest、图标与 Service Worker，可安装独立窗口。
- **热力图**：首页写作活动（字 / 块 / 篇）。
- **账号与 Agent API**：长效 `hub_` API Token；鉴权支持登录 JWT、`hub_` Token、HTTP Basic；账号页说明与示例。
- **子账号 / 成员**：个人空间可创建子账号；团队空间成员角色管理。
- **AI 设置**：「刷新模型列表」代理上游 models，可选 chat / embedding 模型。
- **文档 ACL**：笔记级可见性（空间 / 所有者 / 指定成员）。
- **飞书**：用户扫码 OAuth 为主路径；知识库链接解析；用户身份同步知识库与我的文档库。
- **Notion**：OAuth + Integration Token；明确「仅已分享给 Integration 的页」同步范围；路径/树与规范化改进。
- **环境与运维**：`.env.example` 补齐飞书 OAuth 变量；迁移 `0011`–`0014`（氛围设置、note_links、api_tokens、索引等）。
- **文档**：重写中文 README、新增用户指南；开发文档标记为已实现并指向 README。
